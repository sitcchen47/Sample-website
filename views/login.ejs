<!DOCTYPE html>
<html>
  <head>
    <title>登录</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <form action="/login" method="post">
        <label for="loginname">用户名
            <input type="text" name="loginname" id="loginUsername">
        </label>
        <label for="password">密码
            <input type="password" name="password" id="loginPsd">
        </label>
        <p>验证码 <img src="/random-confirm-pic" alt="验证码图片"> 
            <input type="text" name="confirmPic" id="picForConfirm">
        </p>
        <button type="submit">登录</button>
    </form>
    <script>
        let error = <%- JSON.stringify(error) %>;
        if (error) {
            let spanDom = document.createElement('span');
            spanDom.innerText = error.message;
            let targetInputDom = document.querySelector(`input[name=${error.position}]`);
            targetInputDom.insertAdjacentElement("afterend", spanDom);
        }
    </script>
    <script>
        // ajax共用一套验证表单机制 还是 onchange触发事件
    </script>
  </body>
</html>